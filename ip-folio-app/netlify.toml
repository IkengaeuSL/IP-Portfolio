[build]
  command = "npm run build"
  publish = "dist"
  functions = "netlify/functions"

# Las SPA sirven index.html para cualquier ruta
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
